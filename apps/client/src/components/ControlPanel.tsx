import React from 'react';

export const ControlPanel: React.FC = () => {
  return (
    <div style={{ padding: '16px', backgroundColor: 'var(--bg-secondary)', borderRadius: '8px', marginTop: 'auto' }}>
      <h3 style={{ marginBottom: '8px', fontSize: '1rem', color: 'var(--text-secondary)' }}>Controles</h3>
      {/* Controles de áudio e configurações futuras aqui */}
    </div>
  );
};
