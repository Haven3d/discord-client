const fs = require('fs'); let c = fs.readFileSync('PainelServidor.js', 'utf8'); c = c.replace('let isRunning = false;', 'let isRunning = false; let authEnabled = false;').replace('isRunning = data.isRunning;', 'isRunning = data.isRunning; authEnabled = data.authEnabled;').replace('const urlBox = document.getElementById(''url-box'');', 'const authBtn = document.getElementById(''auth-btn'');\\nconst urlBox = document.getElementById(''url-box'');').replace('if (data.logs && data.logs.length > 0) {', 'if (authEnabled) { authBtn.innerHTML = ''Auth: Ligado''; authBtn.style.backgroundColor = ''#43b581''; } else { authBtn.innerHTML = ''Auth: Desligado''; authBtn.style.backgroundColor = ''#7289da''; }\\n                if (data.logs && data.logs.length > 0) {'); fs.writeFileSync('PainelServidor.js', c);
